# CloudPath labs
To learn enough about DevOps in next 33 lessons and decide whether it's a path for me.

## Week 1

My IAM policy allows an identity to read objects from the specified S3 bucket using `s3:GetObject`. It follows the principle of least privilege because it grants only the permission needed to read objects instead of allowing unnecessary actions.
